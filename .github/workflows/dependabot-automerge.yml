name: "Dependabot Auto Approve and Merge"
on:
  pull_request_target:
    types: [opened, synchronize, reopened]

jobs:
  call-automerge:
    uses: flowlogix/base-pom/.github/workflows/dependabot-automerge.yml@9a1ac51dd86eb001faf318271d5e96653853f981 # v139
    secrets:
      github-token: ${{ secrets.GH_AUTOMERGE_TOKEN }}
