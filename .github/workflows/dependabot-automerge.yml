name: "Dependabot Auto Approve and Merge"
on:
  pull_request_target:
    types: [opened, synchronize, reopened]

jobs:
  call-automerge:
    uses: flowlogix/base-pom/.github/workflows/dependabot-automerge.yml@322f81e61d0c603a1cc5b867e2af836918a87e82 # 139
    secrets:
      github-token: ${{ secrets.GH_AUTOMERGE_TOKEN }}
